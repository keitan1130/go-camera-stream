version: '3.8'
services:
  camera-stream:
    build: .
    ports:
      - "${APP_PORT:-8080}:${APP_PORT:-8080}"
    environment:
      - PORT=${APP_PORT:-8080}
    restart: unless-stopped
    tty: true
