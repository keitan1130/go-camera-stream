services:
  camera-stream:
    build: .
    network_mode: "host"
    environment:
      - APP_PORT=${APP_PORT:-8080}
      - PUBLIC_HOST=${PUBLIC_HOST:-localhost}
    restart: unless-stopped
    tty: true
